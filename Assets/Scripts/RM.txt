tree("Root")
	parallel
		repeat mute tree("Alive")
		repeat mute tree("Dead")

tree("Alive")
	fallback
		while IsInRange	
			tree("InRange")	
		Advance


tree("InRange")
	fallback
		Not
			Random 1 2 3
				Wait 0.5
				Wait 1.0
				Wait 1.5
		Shoot
		Pursue

tree("Dead")
	while isDead
		Die
